module.exports = function() {
	return {
		/**
		 * Formats a citation object to a string based on set format options
		 * @param {Citation} citation to format
		 * @param {Array} array of format options
		 */
		'format' : function(citation, formatOptions) {
			// TODO: Generate citation
			return 'Citation'
		}
	}
}()